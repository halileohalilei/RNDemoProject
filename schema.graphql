schema {
  query: Query
  mutation: MutationType
}

# Account type
type Account implements Node {
  id: ID!
  name: String
  isOfficial: Boolean
  slug: String
  createdAt: String
  updatedAt: String
  about: String
  twitter: String
  facebook: String
  behance: String
  isActive: Boolean
  city: String
  avatarUrl: String
  website: String
  displayName: String
  shareUrl: String
  country: Country
  imessageAppIds: [String]
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  imessageApps(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): ImessageConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
}

type AccountConnection {
  # A list of edges.
  edges: [AccountEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type AccountEdge {
  # The item at the end of the edge.
  node: Account

  # A cursor for use in pagination.
  cursor: String!
}

# Banner type
type Banner implements Node {
  id: ID!
  name: String
  image_url: String
  target_link: String
  country: Country
}

type BannerConnection {
  # A list of edges.
  edges: [BannerEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type BannerEdge {
  # The item at the end of the edge.
  node: Banner

  # A cursor for use in pagination.
  cursor: String!
}

# Autogenerated input type of BuyCoinsPackage
input BuyCoinsPackageInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  iosStoreId: ID!
  appStoreTransactionId: ID
  appStoreReceipt: ID
  authenticationToken: String!
}

# Autogenerated return type of BuyCoinsPackage
type BuyCoinsPackagePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User

  # Deprecated. Please use `user` type.
  transaction: UserTransaction
}

# Autogenerated input type of BuyPackageFromReceipt
input BuyPackageFromReceiptInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  appStoreReceipt: ID!
  authenticationToken: String
  packageId: String!
}

# Autogenerated return type of BuyPackageFromReceipt
type BuyPackageFromReceiptPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User

  # Deprecated. Please use `user` type.
  transaction: UserTransaction
}

# Autogenerated input type of BuyPackage
input BuyPackageInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  packageId: ID!
  authenticationToken: String!
}

# Autogenerated return type of BuyPackage
type BuyPackagePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User

  # Deprecated. Please use `user` type.
  transaction: UserTransaction
}

# Autogenerated input type of BuySticker
input BuyStickerInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  stickerId: ID!
  authenticationToken: String!
}

# Autogenerated return type of BuySticker
type BuyStickerPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User

  # Deprecated. Please use `user` type.
  transaction: UserTransaction
}

# Category type
type Category implements Node {
  id: ID!
  isOfficial: Boolean
  isActive: Boolean
  parentId: Int
  slug: String
  createdAt: String
  updatedAt: String
  name: String
  itunes_name: String
  parent: Category
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  popularPackages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
  childs(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CategoryConnection
}

type CategoryConnection {
  # A list of edges.
  edges: [CategoryEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type CategoryEdge {
  # The item at the end of the edge.
  node: Category

  # A cursor for use in pagination.
  cursor: String!
}

# CoinsPackage type
type CoinsPackage implements Node {
  id: ID!
  android_store_id: String
  ios_store_id: String
  name: String
  slug: String
  coins: Int
  price: Float
  isActive: Boolean
  updatedAt: String
  iosStoreId: String
  androidStoreId: String
}

type CoinsPackageConnection {
  # A list of edges.
  edges: [CoinsPackageEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type CoinsPackageEdge {
  # The item at the end of the edge.
  node: CoinsPackage

  # A cursor for use in pagination.
  cursor: String!
}

# Complaint type
type Complaint implements Node {
  id: ID!
}

# Country type
type Country implements Node {
  id: ID!
  name: String
  code: String
  createdAt: String
  updatedAt: String
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
}

type CountryConnection {
  # A list of edges.
  edges: [CountryEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type CountryEdge {
  # The item at the end of the edge.
  node: Country

  # A cursor for use in pagination.
  cursor: String!
}

# Autogenerated input type of CreateComplaint
input CreateComplaintInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  authenticationToken: String!
  packageId: String
  stickerId: String
  reason: String
}

# Autogenerated return type of CreateComplaint
type CreateComplaintPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  complaint: Complaint
}

# Autogenerated input type of CreateDevice
input CreateDeviceInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  authenticationToken: String!
  deviceToken: String!
  appIdentifier: String!
  platform: String!
}

# Autogenerated return type of CreateDevice
type CreateDevicePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  device: Device
}

# Autogenerated input type of CreateSubscriptionFromReceipt
input CreateSubscriptionFromReceiptInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  appStoreReceipt: ID
  authenticationToken: String!
  platform: String!
}

# Autogenerated return type of CreateSubscriptionFromReceipt
type CreateSubscriptionFromReceiptPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Autogenerated input type of CreateSubscription
input CreateSubscriptionInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String

  # User auth token
  authenticationToken: String!

  # Plan slug. Available slugs: unlimited
  plan: String!

  # Format: Year-month-day => 2016-02-01
  startDate: String!

  # Format: Year-month-day => 2016-03-01
  endDate: String!
}

# Autogenerated return type of CreateSubscription
type CreateSubscriptionPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  subscription: Subscription
}

# Autogenerated input type of CreateUser
input CreateUserInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  password: String!
  phone: String!
  email: String!
  name: String!
}

# Autogenerated return type of CreateUser
type CreateUserPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Autogenerated input type of DeleteDevice
input DeleteDeviceInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  authenticationToken: String!
  deviceToken: String!
  appIdentifier: String!
  platform: String!
}

# Autogenerated return type of DeleteDevice
type DeleteDevicePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  device: Device
}

# Device type
type Device implements Node {
  id: ID!
}

# Frame type
type Frame implements Node {
  id: ID!
  stickerId: Int
  createdAt: String
  updatedAt: String
  fileUrl(width: Int, height: Int, format: String, watermarked: Boolean, fullWatermarked: Boolean): String
  sticker: Sticker
}

type FrameConnection {
  # A list of edges.
  edges: [FrameEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type FrameEdge {
  # The item at the end of the edge.
  node: Frame

  # A cursor for use in pagination.
  cursor: String!
}

# Gift type
type Gift implements Node {
  id: ID!
  receiverId: Int
  senderId: Int
  packageId: Int
  stickerId: Int
  isReceived: Boolean
  name: String
  createdAt: String
  updatedAt: String
  sender(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): UserConnection
  receiver(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): UserConnection
  package(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  sticker(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
}

type GiftConnection {
  # A list of edges.
  edges: [GiftEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type GiftEdge {
  # The item at the end of the edge.
  node: Gift

  # A cursor for use in pagination.
  cursor: String!
}

# Autogenerated input type of GuestUser
input GuestUserInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
}

# Autogenerated return type of GuestUser
type GuestUserPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Imessage type
type Imessage implements Node {
  id: ID!
  identifier: String
  url: String
  price: Float
  version: String
  latestBuildNumber: Int
  liveVersion: String
  editVersion: String
  age_ratings: JSONType
  buildNumber: Int
  globalState: String
  appleId: Int

  # Deprecated !!! Dont use it. We will remove it in next version.
  icon: String
  webMetaImage: String
  package: Package
  webIphone: String
  categories: [String]
  imessageAppIcon: String
  translations(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackagePlatformTranslationConnection
}

type ImessageConnection {
  # A list of edges.
  edges: [ImessageEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type ImessageEdge {
  # The item at the end of the edge.
  node: Imessage

  # A cursor for use in pagination.
  cursor: String!
}

# Payment object
input IosPayment {
  productIdentifier: String!
  quantity: Int!
  requestData: String!
  applicationUsername: String!
  simulatesAskToBuyInSandbox: Boolean!
}

# Transaction object
input IosTransaction {
  transactionIdentifier: String!
  transactionDate: String!
  transactionState: String!
  payment: IosPayment!
}

# A JSON object
scalar JSONType

# Language type
type Language implements Node {
  id: ID!
  name: String
  code: String
  createdAt: String
  updatedAt: String
}

type LanguageConnection {
  # A list of edges.
  edges: [LanguageEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type LanguageEdge {
  # The item at the end of the edge.
  node: Language

  # A cursor for use in pagination.
  cursor: String!
}

# Line type
type Line implements Node {
  id: ID!
  identifier: String
  url: String
  price: Float
  version: String
  latestBuildNumber: Int
  liveVersion: String
  editVersion: String
  buildNumber: Int
  globalState: String
}

# Location type
type Location implements Node {
  id: ID!
  name: String
  slug: String
  coordinates: JSONType
}

type LocationConnection {
  # A list of edges.
  edges: [LocationEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type LocationEdge {
  # The item at the end of the edge.
  node: Location

  # A cursor for use in pagination.
  cursor: String!
}

# MarketingPicture type
type MarketingPicture implements Node {
  id: ID!
  fileUrl: String
  title: String
  package: Package
}

type MarketingPictureConnection {
  # A list of edges.
  edges: [MarketingPictureEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type MarketingPictureEdge {
  # The item at the end of the edge.
  node: MarketingPicture

  # A cursor for use in pagination.
  cursor: String!
}

type MutationType {
  createUser(input: CreateUserInput!): CreateUserPayload
  updateUser(input: UpdateUserInput!): UpdateUserPayload
  createGuest(input: GuestUserInput!): GuestUserPayload
  updateAvatar(input: UpdateAvatarInput!): UpdateAvatarPayload
  updatePhone(input: UpdatePhoneInput!): UpdatePhonePayload
  verifyPhone(input: VerifyPhoneInput!): VerifyPhonePayload
  sendVerificationCode(input: SendVerificationCodeInput!): SendVerificationCodePayload
  buyPackage(input: BuyPackageInput!): BuyPackagePayload
  buyPackageFromReceipt(input: BuyPackageFromReceiptInput!): BuyPackageFromReceiptPayload
  buySticker(input: BuyStickerInput!): BuyStickerPayload
  buyCoinsPackage(input: BuyCoinsPackageInput!): BuyCoinsPackagePayload
  restoreFromPurchases(input: RestoreFromPurchasesInput!): RestoreFromPurchasesPayload
  restorePurchases(input: RestorePurchasesInput!): RestorePurchasesPayload
  createComplaint(input: CreateComplaintInput!): CreateComplaintPayload
  createDevice(input: CreateDeviceInput!): CreateDevicePayload
  deleteDevice(input: DeleteDeviceInput!): DeleteDevicePayload
  createSubscription(input: CreateSubscriptionInput!): CreateSubscriptionPayload
  createSubscriptionFromReceipt(input: CreateSubscriptionFromReceiptInput!): CreateSubscriptionFromReceiptPayload
  updateImessage(input: UpdateImessageInput!): UpdateImessagePayload
  updateLineByPackage(input: UpdateLineByPackageInput!): UpdateLineByPackagePayload
  updateLineByIdentifier(input: UpdateLineByIdentifierInput!): UpdateLineByIdentifierPayload
}

# An object with an ID.
interface Node {
  # ID of the object.
  id: ID!
}

# Package type
type Package implements Node {
  id: ID!
  accountId: Int
  coins: Int
  packageStateId: Int
  copyright: String
  slug: String
  soldCount: Int
  downloadCount: Int
  createdAt: String
  updatedAt: String
  defaultStickerId: Int
  isOfficial: Boolean
  isActive: Boolean
  isAnimated: Boolean
  versionId: Int
  name: String
  description: String
  shareUrl: String
  firebaseConfig: String
  press(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): MarketingPictureConnection
  tags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): TagConnection
  imessage: Imessage
  line: Line
  defaultSticker: Sticker
  account: Account
  packageState: PackageState
  defaultLocale: String
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
  gifts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): GiftConnection
  countries(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CountryConnection
  categories(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CategoryConnection
  systemTags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): SystemTagConnection
}

type PackageConnection {
  # A list of edges.
  edges: [PackageEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type PackageEdge {
  # The item at the end of the edge.
  node: Package

  # A cursor for use in pagination.
  cursor: String!
}

# PackagePlatformTranslation type
type PackagePlatformTranslation implements Node {
  id: ID!
  locale: String
  name: String
  description: String
  keywords: String
  release_notes: String
  iphoneScreenshots: [String]
  ipadScreenshots: [String]
}

type PackagePlatformTranslationConnection {
  # A list of edges.
  edges: [PackagePlatformTranslationEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type PackagePlatformTranslationEdge {
  # The item at the end of the edge.
  node: PackagePlatformTranslation

  # A cursor for use in pagination.
  cursor: String!
}

# PackageState type
type PackageState implements Node {
  id: ID!
  name: String
  slug: String
  createdAt: String
  updatedAt: String
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
}

# Information about pagination in a connection.
type PageInfo {
  # Indicates if there are more pages to fetch
  hasNextPage: Boolean!

  # Indicates if there are any pages prior to the current page
  hasPreviousPage: Boolean!

  # When paginating backwards, the cursor to continue
  startCursor: String

  # When paginating forwards, the cursor to continue
  endCursor: String
}

# PhoneNumber type
type PhoneNumber implements Node {
  id: ID!
  phone: String!
  verified: Boolean!
}

# Plan type
type Plan implements Node {
  id: ID!
  updated_at: String
  created_at: String
  slug: String
  price_per_month: Float
  coins_per_month: Int
  name: String
}

# The query root of this schema
type Query implements Node {
  id: ID!

  # Fetches an object given its ID.
  node(
    # ID of the object.
    id: ID!
  ): Node
  viewer: Viewer
  account(id: ID!): Account
  sticker(id: ID!): Sticker
  tag(id: ID, slug: String): Tag
  systemTag(id: ID, slug: String): SystemTag
  category(id: ID, slug: String): Category
  coinsPackage(id: ID, slug: String): CoinsPackage
  userTransaction(id: ID!): UserTransaction
  language(id: ID!): Language
  country(id: ID!): Country
  marketingPicture(id: ID, slug: String): MarketingPicture
  accountConfirmation(email: String!, authenticationToken: String!): Account
  package(id: ID, slug: String, itunesId: String): Package
  user(authenticationToken: String): User
  systemTags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): SystemTagConnection
  locations(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): LocationConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
    taggedWith: [String]
  ): StickerConnection
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
    isOfficial: Boolean
    isFree: Boolean
  ): PackageConnection
  languages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): LanguageConnection
  gifts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): GiftConnection
  countries(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CountryConnection
  banners(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): BannerConnection
  coinsPackages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CoinsPackageConnection
  categories(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CategoryConnection
  accounts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    isOfficial: Boolean
  ): AccountConnection
  tags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
  ): TagConnection
  searchTerms(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
  ): SearchTermConnection

  # Deprecated! Please use stickers connection.
  keyboardStickersSearch(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String!
  ): StickerConnection
  popularTags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
  ): TagConnection
  stickersForPicture(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String

    # encodedImage decoded from base64. Example: 'data:image/jpeg;base64,/9j/4QFI...'
    encodedImage: String
  ): StickerConnection
}

# Autogenerated input type of RestoreFromPurchases
input RestoreFromPurchasesInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  appStoreReceipt: ID
  authenticationToken: String!
}

# Autogenerated return type of RestoreFromPurchases
type RestoreFromPurchasesPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Autogenerated input type of RestorePurchases
input RestorePurchasesInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  appStoreReceipt: ID
  transactions: [IosTransaction!]!
  authenticationToken: String!
}

# Autogenerated return type of RestorePurchases
type RestorePurchasesPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# SearchTerm type
type SearchTerm implements Node {
  id: ID!
  name: String
}

type SearchTermConnection {
  # A list of edges.
  edges: [SearchTermEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type SearchTermEdge {
  # The item at the end of the edge.
  node: SearchTerm

  # A cursor for use in pagination.
  cursor: String!
}

# Autogenerated input type of SendVerificationCode
input SendVerificationCodeInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  phone: String!
}

# Autogenerated return type of SendVerificationCode
type SendVerificationCodePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  phoneNumber: PhoneNumber
}

# Sticker type
type Sticker implements Node {
  id: ID!
  coins: Int
  packageId: Int
  soldCount: Int
  downloadCount: Int
  createdAt: String
  updatedAt: String
  isOfficial: Boolean
  isActive: Boolean
  isAnimated: Boolean
  timing: String
  fps: Int
  duration: Int
  repetitions: Int
  framesOrder: String

  # | Style        | Dimensions           | Available Content Types  |
  # | ------------- |:-------------:| -----:|
  # | fixed_width_and_height | 200x200 | png,mp4,gif,webp|
  # | fixed_width_and_height_downsampled | 200x200 | png,mp4,gif,webp|
  # | fixed_width_and_height_small | 100x100 | png,gif,webp|
  # | fixed_width_and_height_medium | 320x320 | png,mp4,gif,webp|
  # | fixed_width_and_height_medium_downsampled | 320x320 | png,mp4,gif,webp|
  # | imessage_small | 300x300 | png,mp4|
  # | imessage_medium | 408x408 | png|
  # | imessage_large | 618x618 | png|
  images: JSONType

  # DEPRECATED!!! Please use images node.
  fileUrl(
    width: Int
    height: Int
    format: String
    watermarked: Boolean
    fullWatermarked: Boolean

    # A specific RGB color. Example: dd14d1
    backgroundColor: String
  ): String
  fileUrlWatermarked(width: Int, height: Int): String
  previewImageData: String
  package: Package
  frames(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): FrameConnection
  gifts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): GiftConnection
  tags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): TagConnection
  system_tags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): SystemTagConnection
}

type StickerConnection {
  # A list of edges.
  edges: [StickerEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type StickerEdge {
  # The item at the end of the edge.
  node: Sticker

  # A cursor for use in pagination.
  cursor: String!
}

# Subscription type
type Subscription implements Node {
  id: ID!
  updated_at: String
  created_at: String
  end_time: String
  start_time: String
  plan_id: Int
  user_id: Int
  user: User
  plan: Plan
}

# SystemTag type
type SystemTag implements Node {
  id: ID!
  name: String
  slug: String
  parentId: Int
  parent: SystemTag
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
    isOfficial: Boolean
    isFree: Boolean
  ): PackageConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection

  # Popular accounts
  accounts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): AccountConnection
  childs(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): SystemTagConnection
}

type SystemTagConnection {
  # A list of edges.
  edges: [SystemTagEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type SystemTagEdge {
  # The item at the end of the edge.
  node: SystemTag

  # A cursor for use in pagination.
  cursor: String!
}

# Tag type
type Tag implements Node {
  id: ID!
  locale: String
  name: String
  taggingsCount: Int
  isActive: Boolean
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
}

type TagConnection {
  # A list of edges.
  edges: [TagEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type TagEdge {
  # The item at the end of the edge.
  node: Tag

  # A cursor for use in pagination.
  cursor: String!
}

# TransactionTyepe type
type TransactionType implements Node {
  id: ID!
  name: String
  slug: String
  transactionValue: Int
  createdAt: String
  updatedAt: String
}

type TransactionTypeConnection {
  # A list of edges.
  edges: [TransactionTypeEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type TransactionTypeEdge {
  # The item at the end of the edge.
  node: TransactionType

  # A cursor for use in pagination.
  cursor: String!
}

# Autogenerated input type of UpdateAvatar
input UpdateAvatarInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  authenticationToken: String!
  encodedAvatar: String!
}

# Autogenerated return type of UpdateAvatar
type UpdateAvatarPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Autogenerated input type of UpdateImessage
input UpdateImessageInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String

  # Slug of package
  packageSlug: String!

  # Bundle identifier
  identifier: String

  # Bundle version
  version: String

  # Itunes url
  url: String
}

# Autogenerated return type of UpdateImessage
type UpdateImessagePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  imessage: Imessage
}

# Autogenerated input type of UpdateLineByIdentifier
input UpdateLineByIdentifierInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String

  # Line ID
  identifier: String

  # State
  state: String
}

# Autogenerated return type of UpdateLineByIdentifier
type UpdateLineByIdentifierPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  line: Line
}

# Autogenerated input type of UpdateLineByPackage
input UpdateLineByPackageInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String

  # Slug of package
  packageSlug: String!

  # You know what is verification code ?
  verificationCode: String!

  # Line ID
  identifier: String

  # Version
  version: String

  # State
  state: String

  # Itunes url
  url: String
}

# Autogenerated return type of UpdateLineByPackage
type UpdateLineByPackagePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  line: Line
}

# Autogenerated input type of UpdatePhone
input UpdatePhoneInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  phone: String
  authenticationToken: String!
}

# Autogenerated return type of UpdatePhone
type UpdatePhonePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Autogenerated input type of UpdateUser
input UpdateUserInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  email: String
  name: String
  authenticationToken: String!
}

# Autogenerated return type of UpdateUser
type UpdateUserPayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# User type
type User implements Node {
  id: ID!
  name: String
  email: String
  phone: String
  isVerified: Boolean
  country: String
  apiKey: String
  balance: Int
  createdAt: String
  updatedAt: String
  authenticationToken: String
  hasValidSubscription: Boolean
  subscriptionPlatform: String
  subscriptionEndDate: String
  subscriptionStartDate: String
  avatarUrl: String
  isGuest: Boolean
  transactions(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): UserTransactionConnection
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
  received_gifts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): GiftConnection
  sent_gifts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): GiftConnection
}

type UserConnection {
  # A list of edges.
  edges: [UserEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type UserEdge {
  # The item at the end of the edge.
  node: User

  # A cursor for use in pagination.
  cursor: String!
}

# UserTransaction type
type UserTransaction implements Node {
  id: ID!
  coinsPackageId: Int
  packageId: Int
  stickerId: Int
  userId: Int
  transactionTypeId: Int
  coins: Int
  description: String
  price: Float
  createdAt: String
  updatedAt: String
  accountId: Int
  coins_package(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CoinsPackageConnection
  package(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): PackageConnection
  sticker(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): StickerConnection
  user(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): UserConnection
  transaction_type(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): TransactionTypeConnection
  account(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): AccountConnection
}

type UserTransactionConnection {
  # A list of edges.
  edges: [UserTransactionEdge]

  # Information to aid in pagination.
  pageInfo: PageInfo!
  totalCount: Int
}

# An edge in a connection.
type UserTransactionEdge {
  # The item at the end of the edge.
  node: UserTransaction

  # A cursor for use in pagination.
  cursor: String!
}

# Autogenerated input type of VerifyPhone
input VerifyPhoneInput {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  verifyCode: String!
  phone: String!
  guestAuthenticationToken: String!
}

# Autogenerated return type of VerifyPhone
type VerifyPhonePayload {
  # A unique identifier for the client performing the mutation.
  clientMutationId: String
  user: User
}

# Unassociated viewer object queries
type Viewer implements Node {
  id: ID!
  systemTags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): SystemTagConnection
  locations(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): LocationConnection
  stickers(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
    taggedWith: [String]
  ): StickerConnection
  packages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
    isOfficial: Boolean
    isFree: Boolean
  ): PackageConnection
  languages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): LanguageConnection
  gifts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): GiftConnection
  countries(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CountryConnection
  banners(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): BannerConnection
  coinsPackages(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CoinsPackageConnection
  categories(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
  ): CategoryConnection
  accounts(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    isOfficial: Boolean
  ): AccountConnection
  tags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
  ): TagConnection
  searchTerms(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
  ): SearchTermConnection

  # Deprecated! Please use stickers connection.
  keyboardStickersSearch(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String!
  ): StickerConnection
  popularTags(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String
    query: String
  ): TagConnection
  stickersForPicture(
    # Returns the first _n_ elements from the list.
    first: Int

    # Returns the elements in the list that come after the specified global ID.
    after: String

    # Returns the last _n_ elements from the list.
    last: Int

    # Returns the elements in the list that come before the specified global ID.
    before: String

    # encodedImage decoded from base64. Example: 'data:image/jpeg;base64,/9j/4QFI...'
    encodedImage: String
  ): StickerConnection
}